# Commitments from redesign-v3-v8

Generated by Phase 5.5 (deferral defense). **No commitments from this bundle** — the Phase 4/5 review
battery (`wf_1daa7d60-fc4`) confirmed 9 findings (2 BLOCKER, 5 MAJOR, 2 MINOR) and all 9 landed as
`applyInline` fixes in this PR. 0 `forcedApply`, 0 `deferralsArchitecture`, 0 `deferralsBlocked`,
0 `unverifiedDeferred`, 0 `escalations`. 1 finding refuted.

**This is the last bundle of the redesign-v3 plan.** V0–V6 and V8 are all shipped; V7 (real team
photos) was removed from the plan's §2 table at launch because it is gated on D2 and no photos were
supplied — it is deferred, not dropped, and the HANDOFF §6 D2 entry keeps the reference alive.

## Target: (none — final bundle of this plan)

_(none)_

## Target: long-term backlog

_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)

Bruno's calls. None block anything; the plan is complete without them.

- **The live Trello `[TEMPLATE] Codirity Client Board` still promises "75% back."** Open since V5.
  The seeding script and Appendix B were corrected, but `copyBoard` copies the ALREADY-SEEDED board,
  so every newly onboarded client lands in a board contradicting the site's 50%. Only a manual edit
  in Trello closes this. **This is the one genuinely user-facing loose end left by the whole plan.**
- **+6.7 KB gzipped on the homepage document** is what the case-studies section costs (finding #6 —
  the RSC flight payload, not the JS bundle). Real, and worth knowing rather than assuming the
  section was free. If page weight matters more than the proof does, the two inline SVG sketches are
  the first thing to trim.
- **Publishing the pause/cancel billing mechanics** ("31-day cycles, unused days banked") — real,
  approved internally, more generous than what the site says, but publishing it is a new public
  financial commitment.
- **Three book-a-call prompts stack in a row** (FAQ header line, FAQ button, closing band). All live
  and instrumented, so none were removed unilaterally.
- **`Footer.tsx`** still carries pre-redesign copy ("AI-powered automation and system development
  that transforms businesses and accelerates growth") and renders the brand mark in `font-mono`
  although V0 retired Space Mono. It was never any bundle's surface in this plan.
- **`Hero.tsx`** still hasn't migrated to the shared `AccentWord` (outstanding since V2).
- **V7 (the people / real engineer photos)** remains gated on D2. Re-add it as a real §2 row with a
  §3 launch command whenever photos exist.

## Amendments to prior commitments

(none — this bundle inherited 0 commitments; every prior bundle in the plan recorded "(none)"
against the V8 target, and the only `STATUS: OPEN` item elsewhere in the repo belongs to the
client-onboarding plan.)
