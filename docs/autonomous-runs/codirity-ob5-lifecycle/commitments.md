# Commitments from codirity-ob5-lifecycle

Generated by Phase 5.5 (deferral defense). IDs carry an immutable origin-bundle prefix (B5).

**This bundle (client-onboarding Bundle 5, the FINAL bundle of the plan) produced ZERO new
deferrals.** The review battery (`wf_c04ea09e-993`, verify-voters 3) returned 3 MAJOR + 10 MINOR
confirmed findings — all applied inline and re-verified (lint/tsc/build green; the review's own
type-safety claim was independently re-verified via a standalone `tsc --strict` check rather than
trusted on faith; the acceptance criterion that had never been demonstrated green was closed via a
disposable Ethereal test SMTP account, not deferred or hand-waved). 0 findings refuted, 0
escalations, 0 forced-apply disagreements.

## Target: (none — no new deferrals from this bundle)
_(none)_

## Target: long-term backlog
_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)

Not deferred code work — actions only Bruno can take:

- **Register a persistent Stripe TEST-mode webhook endpoint.** Verified via the Stripe API that
  ZERO webhook endpoint objects exist on this account — every test across Bundles 1-5 used the
  ephemeral `stripe listen --forward-to` CLI tunnel, never a real Dashboard/API Endpoint. O6 Stage 1
  (HANDOFF §4) has not actually happened yet in the way that section describes. Register the
  endpoint against the real Preview/Test deployment URL with all FOUR event types enabled:
  `checkout.session.completed`, `customer.subscription.deleted`, `customer.subscription.updated`,
  `customer.subscription.paused` — otherwise this bundle's entire lifecycle flow is a silent no-op
  outside local dev.
- **Later, O6 Stage 2 (prod endpoint)** — same four event types, registered only after the above is
  confirmed working and per the HANDOFF's existing two-stage gating.
- **Gmail SMTP (`support@codirity.com`) is still broken in production** (`534 5.7.9
  WebLoginRequired`), carried from Bundle 4 — independent of this bundle but affects prod now. This
  bundle's acceptance was re-verified green using a disposable Ethereal test account instead, so the
  code is proven correct; only the real credential needs Bruno's attention.
- **`RESEND_API_KEY` still not set** — carried from Bundle 3, affects the welcome-email step of the
  signup flow (Bundle 4), not this bundle's lifecycle flow.

## Amendments to prior commitments

- **B4-D-opsidempotency1** [STATUS: COMPLETED-IN-B5] Alert/check-in-card crash-window idempotency,
  deferred from Bundle 4. The CARD half is shipped: `createTrackedCard` (ops.ts) generalizes
  `copyBoard`'s board-reconcile-by-marker pattern to Trello cards, covering BOTH Bundle 4's day-5
  check-in card (`createCheckin`, which gained the `eventId` param this requires) and this bundle's
  revoke-access card (`createRevokeAccessCard`) — verified live through the full route via a
  simulated crash-after-create-before-persist scenario, mirroring Bundle 4's own mandatory
  board-reconcile test. The ALERT half is explicitly NOT given a matching mechanism — a considered
  scope decision (documented in `ops.ts`'s `createTrackedCard` comment and this run's notes.md, not
  a silent gap): nodemailer/SMTP has no send-dedup query API without new persistent infrastructure
  disproportionate to a rare, narrow, founder-only, non-customer-facing residual risk already
  massively narrowed by the existing lease-fence. No open commitments remain for this plan.
