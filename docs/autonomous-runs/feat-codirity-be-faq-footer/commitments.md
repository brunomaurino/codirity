# Commitments from feat-codirity-be-faq-footer

Generated by Phase 5.5 (deferral defense). IDs carry an immutable origin-bundle prefix (B5).

**This bundle (subscription-rebuild Bundle 5) produced NO new deferrals.** The review battery
(`wf_f9d75ad6-7d8`) returned 0 deferrals; the single confirmed MINOR (FAQ answer aria-hidden) was
applied inline.

**This bundle CLOSES the outstanding cross-bundle commitment B1-D-jsonld1.**

## Target: subscription-rebuild Bundle 6 (Analytics + performance + acceptance)
_(none new from this bundle)_

## Target: long-term backlog
_(none — OPERATOR-ASSIGNED ONLY)_

## Amendments to prior commitments

- **B1-D-jsonld1** [STATUS: COMPLETED-IN-B5] Service (with `offers` for both tiers) + FAQPage JSON-LD,
  originally deferred from Bundle A. Shipped in this bundle (`src/components/seo/JsonLd.tsx`:
  `ServiceJsonLd` + `FaqPageJsonLd`), sourced from `offer.tiers` (numeric price) and `offer.faq` —
  the same arrays feeding the on-page pricing + FAQ, so structured data and visible copy cannot drift.
  No open commitments remain for this plan.
