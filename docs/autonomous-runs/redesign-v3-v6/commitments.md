# Commitments from redesign-v3-v6

Generated by Phase 5.5 (deferral defense). **No commitments from this bundle** — the Phase 4/5
review battery (`wf_6352c39e-5cc`) confirmed 11 findings and all 11 landed as `applyInline` fixes in
this PR. 0 `forcedApply` escalations, 0 `deferralsArchitecture`, 0 `deferralsBlocked`,
0 `unverifiedDeferred`, 0 `escalations`. 1 finding refuted (correctly — see notes.md).

## Target: redesign-v3 Bundle V8 (Case studies)

(none)

Carried for awareness, not tracked as deferrals — V8 is the next and last bundle in this plan and
will render on the same page, so it should know:

- **The ink band is one-per-site.** `Section variant="ink"` now exists and the closing CTA uses it
  (HANDOFF-redesign-v3.md §1 rule 4). V8's case-studies section sits between Clients and Pricing,
  mid-page — it must NOT introduce a second dark band; the beat only works because it is unique.
- **`.accent` needs an explicit colour on any dark surface.** `AccentWord` now takes a `className`
  for exactly this (`className="text-white"`); `.accent`'s own `color` declaration beats an
  inherited one. V8's headlines use the `.accent` treatment per its brief — on the light page ground
  the default green is correct and no override is needed, but if any V8 headline lands on a
  blob-gradient or otherwise dark surface, pass the colour explicitly and MEASURE it.
- **`--green-light` is now load-bearing at 4.76:1** on the ink band (0.26 over AA). Do not darken it.
- **`CalPopupButton` takes `analyticsLocation`.** If V8 adds a Cal CTA, label it, or the funnel
  cannot attribute the booking.

## Target: long-term backlog

_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)

These are Bruno's calls, not build decisions. None block V8.

- **The live Trello `[TEMPLATE] Codirity Client Board` still promises "75% back."** Carried forward
  from V5 and still open. The seeding script and Appendix B were corrected, but `copyBoard` copies
  the ALREADY-SEEDED board, so every newly onboarded client lands in a board contradicting the
  site's 50%. Only a manual edit in Trello closes this.
- **Publishing the pause/cancel billing mechanics.** The onboarding board states "Billing cycles are
  31 days. Pause anytime: unused days are banked and available whenever you return." That is real,
  approved, and MORE generous than anything the public FAQ says. It was deliberately withheld from
  the site because publishing it makes a new public financial commitment to prospects — Bruno's
  decision, not a build one. Say the word and it goes into the pause answer.
- **Three call-to-book prompts now stack in a row**: the FAQ section header's "Still unsure? Book a
  quick call", the FAQ's own Cal button, and the closing band's. All three are live and instrumented,
  so none were removed unilaterally — but two of the three could go if that reads as pushy.
- **`Footer.tsx` still carries pre-redesign copy and a retired font.** "AI-powered automation and
  system development that transforms businesses and accelerates growth" would not pass the §4
  banned-word gate, and the brand mark still renders in `font-mono` although V0 retired Space Mono
  as a system voice. The footer was not V6's surface; it needs either a small dedicated pass or
  folding into whatever ships next.

## Amendments to prior commitments

(none — this bundle inherited 0 commitments; `redesign-v3-v2` and `redesign-v3-v3` both recorded
"(none)" against the V6 target, and the only `STATUS: OPEN` items elsewhere in the repo belong to the
client-onboarding plan.)
