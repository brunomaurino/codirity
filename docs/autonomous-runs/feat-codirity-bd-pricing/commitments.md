# Commitments from feat-codirity-bd-pricing

Generated by Phase 5.5 (deferral defense). IDs carry an immutable origin-bundle prefix (B4).

**This bundle (subscription-rebuild Bundle 4) produced NO deferrals.** The review battery
(`wf_9f0e3697-1e8`) returned 0 deferrals; both confirmed findings (1 MAJOR + 1 MINOR) were applied
inline. The `caseStudies` empty array remains the intentional D5 placeholder (RecentWork hidden until
content exists — the typed structure + section are the deliverable).

Bundle D closes Bundle A's positioning-neutral deferral (layout.tsx metadata + the OG image are now
subscription-forward). The site now leads with the subscription model end-to-end.

## Target: subscription-rebuild Bundle 5 (FAQ + JSON-LD FAQPage + Book a call + Footer)
_(none new from this bundle)_

## Target: long-term backlog
_(none — OPERATOR-ASSIGNED ONLY)_

## Amendments to prior commitments
- **B1-D-jsonld1** [STATUS: OPEN] Service.offers + FAQPage JSON-LD → Bundle E. Still open. Bundle B
  added `priceAmount`/`CURRENCY` and Bundle D confirmed the pricing is live, so Bundle E now has
  everything it needs (tiers with numeric price, faq array) to complete it.
