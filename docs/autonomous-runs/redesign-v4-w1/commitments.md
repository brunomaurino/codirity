# Commitments from redesign-v4-w1

Generated by Phase 5.5 (deferral defense). **No commitments from this bundle** — battery
`wf_8719eb7c-d4f` confirmed 11 findings (2 BLOCKER, 5 MAJOR, 4 MINOR) and all 11 landed as
`applyInline` fixes in this PR. 0 deferrals of any category, 0 escalations, 0 refuted.

## Target: redesign-v4 Bundle W2 (terms band)

(none as deferrals — awareness:)

- The hero's primary CTA points at `#pricing` until W2 renames the section anchor to `#terms` —
  when it does, update `hero.primaryCta.href` in `offer.ts` (one field, the hero reads it).
- The folio's `on-light` detection consumes `Section`'s `data-ground` — W2's new band section must
  keep rendering through `Section` (or set the attribute itself) or the folio tone will not flip
  over it.
- **The three-times-bitten boundary lesson binds W2's ledger typography too**: any size derived
  from a longest-line must be MEASURED at the exact breakpoint boundary in-browser, never accepted
  from arithmetic — the mockup was wrong at 980, and this bundle's own first fix was wrong by
  0.59px at exactly 1040.

## Target: redesign-v4 Bundle W6 (retirement sweep)

- The legacy `.reveal`/`animate-*` entrance system is now unused by the hero but still consumed by
  the v3 sections W2–W5 will rework — sweep it once the last consumer is gone (already on W0's
  sweep list; W1 adds: the `.line`/`.fade` TRANSITION variants are now scroll-only, keep them, but
  the hero's `.line-rise`/`.fade-rise` keyframes are permanent v4 system, not sweep targets).

## Target: long-term backlog

_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)

- The hero Cal button emits a bare `call_booked` (byte-parity with main preserved). Labeling it
  `analyticsLocation="hero"` is a one-liner whenever the funnel owner wants the breakdown.
- The live Trello template board still says "75% back" (carried since v3 V5; manual edit only).

## Amendments to prior commitments

- W0's three W1-awareness items are all CLOSED by this bundle: the themeColor interim (hero is now
  the dark ground), the `.d-xl` re-verification (measured at five widths — and it caught two more
  boundary bugs), and the reveal system's first consumers (shipped keyframe-first after the battery
  killed the hydration-gated variant).
