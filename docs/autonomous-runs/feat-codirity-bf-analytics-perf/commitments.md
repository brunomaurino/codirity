# Commitments from feat-codirity-bf-analytics-perf

Generated by Phase 5.5 (deferral defense). IDs carry an immutable origin-bundle prefix (B6).

**This bundle (subscription-rebuild Bundle 6, the FINAL bundle) produced NO deferrals.** The review
battery (`wf_fdb30754-d59`) returned 0 deferrals; all 4 confirmed findings were applied inline
(sonner lazy-load reverted; Cal latch fixed; PricingViewedTracker fixed).

**The subscription-rebuild plan is now COMPLETE — Bundles 0, A, B, C, D, E, F all shipped, and no
open commitments remain across the plan** (B1-D-jsonld1 was closed in Bundle E).

## Target: (none — final bundle, plan complete)
_(none)_

## Target: long-term backlog
_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)
- Perf budget re-baseline: the 150 KB gz hard-gate is below the React 19 + Next 16 + React Compiler
  framework floor (~150 KB). Bruno signed off on merging at 198.3 KB gz. Recommend re-baselining the
  budget to a realistic value (e.g. ~200 KB gz) or a Lighthouse-Perf-based gate.
- pricing_viewed: verify in GA DebugView on the deployed site (couldn't be exercised in the headless
  pane where window.innerHeight === 0). The other 4 GA4 events are verified firing.
- Pre-launch (from the HANDOFF): set real Stripe Payment Links + GA id + NEXT_PUBLIC_SITE_URL in
  Vercel; add /terms with counsel-reviewed copy before taking live payments (D6).

## Amendments to prior commitments
_(none — B1-D-jsonld1 already COMPLETED-IN-B5)_
