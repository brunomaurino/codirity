# Commitments from feat-codirity-ba-seo

Generated by Phase 5.5 (deferral defense). Future bundle Phase 2 reads all sibling
`commitments.md` files to assemble its incoming scope. IDs carry an immutable origin-bundle
prefix (B1).

The review battery (`wf_d48f5463-d66`) returned 0 deferrals; both confirmed MAJOR findings were
applied inline. The single item below is a PLANNED scope deferral from the HANDOFF (Bundle A ships
Organization JSON-LD now; Service.offers + FAQPage need offer.ts + FAQ, which don't exist yet).

## Target: subscription-rebuild Bundle 5 (FAQ + JSON-LD FAQPage + Book a call + Footer)
- **B1-D-jsonld1** [STATUS: OPEN] Complete the `Service` (with `offers` for both tiers) + `FAQPage`
  JSON-LD deferred from Bundle A. Reason this target is right: it depends on `offer.ts` (Bundle B)
  for pricing/tier data and the FAQ content (Bundle E) as the single source of truth; authoring it
  in Bundle A would duplicate strings that do not yet live in the repo. The stable Organization
  block already ships in Bundle A (`src/components/seo/JsonLd.tsx`), and that file is where the
  Service/FAQPage additions belong, importing from `offer.ts`.

## Target: long-term backlog
_(none — OPERATOR-ASSIGNED ONLY)_

## Amendments to prior commitments
_(none — Bundle 0's commitments had no open items)_
