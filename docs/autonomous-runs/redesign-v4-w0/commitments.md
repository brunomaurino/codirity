# Commitments from redesign-v4-w0

Generated by Phase 5.5 (deferral defense). **No commitments from this bundle** — the Phase 4/5
review battery (`wf_205956aa-717`) confirmed 15 findings (8 MAJOR, 7 MINOR) and all 15 landed as
`applyInline` resolutions in this PR (14 edits + 1 documented acceptance). 0 `forcedApply`,
0 `deferralsArchitecture`, 0 `deferralsBlocked`, 0 `unverifiedDeferred`, 0 `escalations`,
0 refuted.

## Target: redesign-v4 Bundle W1 (hero + folio)

(none as formal deferrals — carried for awareness:)

- **`viewport.themeColor` is already `#0A1712`** while the hero is still light (battery finding
  #11, accepted interim). W1 flipping the hero to the dark ground CLOSES this — do not re-point
  the themeColor, just ship the hero.
- **`.d-xl`'s single-line promise must be re-verified against the REAL hero** at 375/980/1280 with
  the real font — the mockup verified it, but W1 is the first live consumer. The curve is the
  mockup's exact `clamp(2.3rem, 10.2vw, 8.4rem)`; do not "improve" it to rem+vw (finding #4 — the
  first attempt did and overflowed at 375px).
- **The reveal system (`.rv`/`.line`/`.fade`) has zero consumers until W1** — W1's hero is its
  first real test, including the 450ms arming delay against the hero's own entrance.

## Target: redesign-v4 Bundle W6 (retirement sweep)

The sweep list this bundle creates (all marked in-code too):

- Delete the `.blob-*`/`.glass-dark` alias rule + `.panel-deep` consumers' interim styling,
  `src/lib/blob.ts`, `src/components/ui/AccentWord.tsx`, `offer.ts`'s `headlineAccent` field.
- Delete the neutralized `dark:` classes tree-wide (~150) and then the `@custom-variant`
  neutralizer itself.
- Delete the INTERIM legacy token aliases in `:root` (green-*, gray ramp, sage, off-white,
  paper-raised, ink-soft) once zero consumers remain.
- Delete the legacy `.reveal`/`animate-*` system once every section is on `.rv`.
- Replace `docs/design-system.md` (currently a SUPERSEDED banner over the historical v3 body).
- The doc-sample class in `design-system.md` was neutralized so Tailwind's content scanner stops
  emitting dead utilities from documentation — when rewriting the doc, keep class samples
  non-scannable or move them out of the scanner's reach.

## Target: long-term backlog

_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)

- **The live Trello `[TEMPLATE] Codirity Client Board` still promises "75% back"** — carried from
  v3 (V5), still open, still only fixable by hand in Trello.

## Amendments to prior commitments

(none — this is the first bundle of the redesign-v4 plan; it inherited 0.)
