# Commitments from redesign-v3-v2

Generated by Phase 5.5 (deferral defense). No commitments from this bundle —
the Phase 4/5 review battery (`wf_a09d23d8-c96`) confirmed 10 findings, all 10
landed as `applyInline` fixes in this PR. 0 `forcedApply` escalations,
0 `deferralsArchitecture`, 0 `deferralsBlocked`, 0 `unverifiedDeferred`.

One outstanding item is noted for awareness, not tracked as a formal
deferral (no owning bundle needs to act on it, but whichever future bundle
next touches `Hero.tsx` should know): `AccentWord.tsx`'s docstring flags
that `Hero.tsx` still carries its own independent (pre-word-boundary-fix)
copy of the same one-word-accent logic rather than using the shared
component. Not a defect in either file today — both work correctly — just
duplicated logic.

## Target: redesign-v3 Bundle V3 (Services)

(none)

## Target: redesign-v3 Bundle V6 (FAQ + final CTA)

(none)

## Target: redesign-v3 Bundle V8 (Case studies)

(none)

## Amendments to prior commitments

(none)
