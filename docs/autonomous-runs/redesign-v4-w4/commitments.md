# Commitments from redesign-v4-w4

Generated by Phase 5.5 (deferral defense). **No commitments from this bundle** — battery
`wf_c3279142-f9a` confirmed 15 findings and refuted 0, and all 15 landed as `applyInline` fixes in
this PR. 0 deferrals, 0 escalations, 0 forced applies.

## Target: redesign-v4 Bundle W5

(none as deferrals — awareness:)

- **An animation referencing undefined `@keyframes` dies SILENTLY.** W4 shipped
  `animation: stat-wipe` with the keyframes never ported: the name resolved to `none`, the wipe
  never existed, AND the `@supports not` fallback was suppressed because the positive branch still
  matched. Nothing errored, and **a visual check cannot catch it** — a wipe that never ran looks
  identical to one that finished. `scripts/w4-css-gate.py` now asserts every referenced
  animation-name has matching keyframes; **run it after any bundle that adds motion.**
- **Hand-set line breaks must be re-derived for the REAL strings, and measured at the gutter
  boundary.** The mockup's `ch` values are calibrated for its own trimmed copy. W4's lines re-wrapped
  between 900px and 959px specifically, because `.wrap-v4` doubles its gutter at 900 — a band a
  spot-check at 1280 and 375 would sail past. W5's ruled service list and FAQ have the same shape.
- **A `.d-*` heading needs `class="display d-md"`** (carried from W3, held here).
- **Use `--chalk-faint` instead of stacking opacity on a colour** (carried from W3; W4 still shipped
  one `.sm-arrow` violation and the review caught it).
- **`.band-ld` / `.band-dl` are now IN USE** — W4 added the light→dark and dark→light gradients
  around the clients/studies run. W5 reworks both neighbours and may reposition or absorb them; do
  not delete them without replacing the transition.

## Target: redesign-v4 Bundle W6 (retirement sweep)

- `scripts/w4-*.py` are permanent gates. `w4-css-gate.py`'s orphaned-animation check is
  **site-wide**, not W4-specific — keep it running after the sweep, since deleting a `@keyframes`
  whose `animation:` reference survives is exactly the failure it detects.
- `sections.caseStudies` is deleted and `sections.recentWork` is now `Pick<SectionCopy,"title">`.
- Unchanged: `.glass-dark` has zero consumers; the `#pricing` alias inside `#terms` is load-bearing.

## Target: long-term backlog

_(none — OPERATOR-ASSIGNED ONLY)_

## Operator-owned follow-ups (recorded, not commitments)

- **Wire `npm test` + CI over `scripts/w2-*`, `w3-*`, `w4-*`.** This is now the highest-leverage
  change available on this repo. W4 shipped a dead flagship deliverable that only an adversarial
  review caught; every gate that would have caught it exists, and nothing runs them automatically.
- **The live Trello `[TEMPLATE] Codirity Client Board` still promises "75% back."** Carried since
  v3 V5; only a manual edit closes it.
- **Page section order is still v3's** — the mockup puts the studies before the clients strip and
  both near the top. A design decision, surfaced but not taken.

## Amendments to prior commitments

- **W3's four W4-awareness items:** three held (the `display d-md` pairing, the flex
  `margin-inline:auto` shrink-wrap, measuring contrast by compositing the full chain — the last of
  which is exactly how the `.sm-arrow` regression was measured). The fourth — "use `--chalk-faint`
  instead of stacking opacity" — was **violated once** in this bundle and caught by review, which is
  the honest record: carrying a lesson forward in a document did not prevent repeating it, and only
  the adversarial pass did.
- **A gate reporting `GATE ARMED` is not proof the gate is sound.** W4's self-test passed 10/10
  while the gate it was testing had four live-demonstrable holes, because the self-test only probed
  the failure modes its author had already thought of. Self-testing bounds a gate from below, never
  from above — the adversarial round is what found the holes, and it should keep being the thing
  that certifies a gate, not the self-test.
